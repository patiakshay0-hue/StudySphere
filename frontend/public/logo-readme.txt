StudySphere logo
================

The app ships with a vector logo (logo.svg) used everywhere by default.

To use your own PNG logo instead (e.g. the full StudySphere artwork), simply
save it in THIS folder as:

    frontend/public/logo.png

The UI automatically prefers logo.png and falls back to logo.svg if it's
missing. No code changes or restart needed — just refresh the browser.
